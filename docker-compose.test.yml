version: "3.9"
services:
  postgres-test:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: ike_test
      POSTGRES_USER: ike
      POSTGRES_PASSWORD: ike_test
    ports:
      - "55432:5432"
    tmpfs:
      - /var/lib/postgresql/data
